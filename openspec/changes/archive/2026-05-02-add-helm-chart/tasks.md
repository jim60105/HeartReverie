## 1. Chart Scaffold

- [x] 1.1 Create `helm/heart-reverie/` directory with subdirectories `templates/` and `examples/`
- [x] 1.2 Write `helm/heart-reverie/Chart.yaml` declaring `apiVersion: v2`, `name: heart-reverie`, `version: 0.1.0`, `appVersion` matching the current `CHANGELOG.md` head, `kubeVersion: ">=1.27.0-0"`, `description`, `type: application`, `home`, `sources`, `maintainers`, and (optional) `icon` pointing at `assets/heart.webp`
- [x] 1.3 Write `helm/heart-reverie/.helmignore` covering `.git/`, `*.md` outside the chart, OS junk, editor swap files, and the repo's `.github/`, `node_modules/`, `coverage/`, `playground/`, `tmp/` directories so `helm package` produces a clean tarball

## 2. Values Surface

- [x] 2.1 Write `helm/heart-reverie/values.yaml` with the full operator-facing surface: `image.{repository,tag,pullPolicy}`; `nameOverride`/`fullnameOverride`; `replicaCount: 1`; `strategy.type: Recreate`; `app.port: 8443` (single source of truth — drives containerPort + Service.targetPort + probe ports + injected `PORT` env); `service.{type,port}`; `ingress.{enabled,className,annotations,hosts,tls}`; `persistence.{enabled,existingClaim,storageClass,accessModes,size,mountPath}`; `prompts.{enabled,mountPath,files}` (default `mountPath: /app/prompt-overrides`, OUTSIDE the playground PVC); `tls.{existingSecret,certKey,keyKey}` (defaults `tls.crt`/`tls.key`); `secret.existingSecret`; `serviceAccount.{create,name,automount}` (defaults `false`/`""`/`false`); `env:` flat map seeded with placeholder `LLM_API_KEY: ""`, `PASSPHRASE: ""` plus inline-commented optional keys (LLM_*, LOG_LEVEL, BACKGROUND_IMAGE, HTTP_ONLY, etc.) — explicitly OMIT `PORT` from the active default since the chart injects it from `app.port`; `extraEnv: []`; `extraVolumes: []`; `extraVolumeMounts: []`; `resources: {}`; `nodeSelector: {}`; `tolerations: []`; `affinity: {}`; `podAnnotations: {}`; `podLabels: {}`; `podSecurityContext.{runAsUser:1000,runAsGroup:0,fsGroup:0,fsGroupChangePolicy:OnRootMismatch}`; `securityContext.{allowPrivilegeEscalation:false,runAsNonRoot:true,capabilities.drop:[ALL]}`; `livenessProbe`/`readinessProbe` defaulting to `tcpSocket` on `app.port` with the documented thresholds
- [x] 2.2 Cross-check every UPPER_SNAKE key listed in `AGENTS.md` Environment Variables table appears as either an active default or an inline-commented hint under `env:` — except `PORT`, which is documented as a comment explaining it is auto-injected from `app.port` and should be set there instead

## 3. Helper Templates

- [x] 3.1 Create `helm/heart-reverie/templates/_helpers.tpl`
- [x] 3.2 Define `heart-reverie.name`, `heart-reverie.fullname` (with `nameOverride`/`fullnameOverride` plus the standard "if release name contains chart name use just release name" branch), `heart-reverie.chart`, `heart-reverie.labels` (full set), `heart-reverie.selectorLabels` (name + instance only)
- [x] 3.3 Define `heart-reverie.secretName` helper that returns `.Values.secret.existingSecret` when set, else `<fullname>-secret` — used by both the Secret template and the Deployment's `envFrom`
- [x] 3.4 Define `heart-reverie.pvcName` helper returning `.Values.persistence.existingClaim` when set, else `<fullname>-data`
- [x] 3.5 Define `heart-reverie.serviceAccountName` helper that returns `.Values.serviceAccount.name` when non-empty, else the chart fullname when `serviceAccount.create` is true, else empty string (so the Deployment can omit `serviceAccountName` and use the namespace `default`)

## 4. Resource Templates

- [x] 4.1 Write `templates/secret.yaml` — guarded by `if not .Values.secret.existingSecret`, named `<fullname>-secret`, iterates `range $key, $value := .Values.env` emitting each as `{{ $key }}: {{ $value | toString | quote }}` and skipping only when `eq $value nil` OR the string-coerced value equals `""`. Append the chart-injected `PORT` entry (string-coerced from `.Values.app.port`) only if `.Values.env.PORT` is unset/empty. Append the chart-injected `PROMPT_FILE` entry only if `prompts.enabled` is true AND `.Values.env.PROMPT_FILE` is unset/empty.
- [x] 4.2 Write `templates/pvc.yaml` — guarded by `if and .Values.persistence.enabled (not .Values.persistence.existingClaim)`, named `<fullname>-data`, carries the `helm.sh/resource-policy: keep` annotation, parameterised access modes and storage size, emits `storageClassName` only when `.Values.persistence.storageClass` is non-empty
- [x] 4.3 Write `templates/configmap-prompts.yaml` — guarded by `if .Values.prompts.enabled`, materialises `.Values.prompts.files` as ConfigMap `data`
- [x] 4.4 Write `templates/serviceaccount.yaml` — guarded by `if .Values.serviceAccount.create`
- [x] 4.5 Write `templates/service.yaml` — type from values, single port mapping `service.port` → `app.port`; selector uses `selectorLabels`
- [x] 4.6 Write `templates/ingress.yaml` — guarded by `if .Values.ingress.enabled`, emits `networking.k8s.io/v1`, copies `ingress.annotations` verbatim, iterates `ingress.hosts[].paths[]` for rules, iterates `ingress.tls` for the TLS block; backend points at the chart-fullname Service on `service.port`
- [x] 4.7 Write `templates/deployment.yaml` — single-container pod with `image`/`pullPolicy` from values; `containerPort: {{ .Values.app.port }}`; `envFrom` referencing the secret name from `heart-reverie.secretName`; `extraEnv` appended as plain `env:` list; volume + volumeMount for playground (gated on `persistence.enabled`); `/certs` volume logic: when `tls.existingSecret` is non-empty render a `secret`-typed volume with `items: [{key: <tls.certKey>, path: cert.pem}, {key: <tls.keyKey>, path: key.pem}]` mounted `readOnly: true`; else render an `emptyDir`; the entire `/certs` mount is gated on `eq (.Values.env.HTTP_ONLY | default "" | toString) "true"` returning false (exact lowercase match — HTTP_ONLY=true skips the mount). ConfigMap mount for prompts at `.Values.prompts.mountPath` (NOT subPath into the playground PVC) when `prompts.enabled`. `extraVolumes`/`extraVolumeMounts` appended last. Pod- and container-level securityContext blocks. `automountServiceAccountToken: {{ .Values.serviceAccount.automount }}`. `serviceAccountName` from `heart-reverie.serviceAccountName` helper, omitted when the helper returns empty. `livenessProbe`/`readinessProbe` defaulting to `tcpSocket: { port: {{ .Values.app.port }} }`. `resources`/`nodeSelector`/`tolerations`/`affinity`. `strategy.type: Recreate`.
- [x] 4.8 Verify the helper for env iteration handles the case where `.Values.env` is nil (no env keys set) without producing an empty `stringData:` block that breaks `helm install`

## 5. Examples and Documentation

- [x] 5.1 Write `helm/heart-reverie/examples/values-traefik.yaml` — sets `ingress.enabled: true`, `ingress.className: traefik`, `env.HTTP_ONLY: "true"` (the Traefik recipe terminates TLS at the controller and runs the pod over plain HTTP, avoiding the need to manage a Traefik `ServersTransport` CRD for self-signed-cert verification), example host + paths + tls block. Include a top-of-file comment block showing the alternative `ServersTransport`-based approach for operators who want in-pod TLS.
- [x] 5.2 Write `helm/heart-reverie/examples/values-nginx.yaml` — sets `ingress.enabled: true`, `ingress.className: nginx`, the ingress-nginx annotations (`nginx.ingress.kubernetes.io/backend-protocol: HTTPS`, `proxy-ssl-verify: off`), example host + paths + tls block (the pod continues to serve HTTPS with self-signed certs since ingress-nginx supports `proxy-ssl-verify: off` directly without a CRD)
- [x] 5.3 Write `helm/heart-reverie/README.md` (English) covering: prerequisites, quick install command, full values reference table including `app.port` and `serviceAccount.*`, single-replica caveat, credential-handling recommendation order (`secret.existingSecret` → `--set-file` → `--set`), Traefik (HTTP_ONLY mode) and nginx (upstream-HTTPS) ingress recipes with copy-paste commands, persistence note (PVC kept on uninstall), prompts caveat (read-only — Prompt Editor saves will fail), TLS section explaining `tls.existingSecret` with `tls.crt`/`tls.key` defaults so a `kubernetes.io/tls` Secret produced by cert-manager works directly, and an "Uninstall" section explaining how to fully remove (including the kept PVC)
- [x] 5.4 Write `docs/helm-deployment.md` (Traditional Chinese) — full deployment guide localised for zh-TW operators, mirroring the chart README's structure and matching the prose style of existing files in `docs/`
- [x] 5.5 Add a "Helm 部署 / Helm Deployment" subsection to the root `README.md` with a one-paragraph summary and links to both `helm/heart-reverie/README.md` and `docs/helm-deployment.md`

## 6. Verification

- [x] 6.1 Run `helm lint helm/heart-reverie` and ensure exit code 0 with no errors
- [x] 6.2 Run `helm template hr helm/heart-reverie --set env.LLM_API_KEY=sk-x --set env.PASSPHRASE=p` and confirm exit 0; pipe through `kubectl apply --dry-run=server -f -` (or `client` if no cluster) to validate the manifests parse against Kubernetes API schema
- [x] 6.3 Render with each example file (`-f helm/heart-reverie/examples/values-traefik.yaml` and `-f helm/heart-reverie/examples/values-nginx.yaml`) and confirm: traefik example produces `HTTP_ONLY: "true"` in the Secret; nginx example produces `nginx.ingress.kubernetes.io/backend-protocol: HTTPS` in the Ingress annotations; both have correct host/tls blocks
- [x] 6.4 Render `--set secret.existingSecret=ext` and grep the output for `<fullname>-secret` — must be absent — and confirm `envFrom.secretRef.name: ext` is present
- [x] 6.5 Render `--set persistence.enabled=false` and confirm no PVC and no `playground` volumeMount are emitted
- [x] 6.6 Render `--set tls.existingSecret=hr-tls` and confirm the `/certs` mount is a `secret`-typed volume with `items: [{key: tls.crt, path: cert.pem}, {key: tls.key, path: key.pem}]`, NOT an emptyDir; render again with `--set tls.certKey=server.crt --set tls.keyKey=server.key` and confirm the `items` keys updated accordingly
- [x] 6.7 Render `--set fullnameOverride=custom` and confirm Deployment name `custom`, Service name `custom`, Secret name `custom-secret`, PVC name `custom-data`
- [x] 6.8 Render with no `env:` overrides at all (just `--set env.LLM_API_KEY=x --set env.PASSPHRASE=y`) and inspect the rendered Secret to ensure (a) empty-valued keys from `values.yaml` were dropped and (b) the chart-injected `PORT: "8443"` entry is present
- [x] 6.9 Render `--set env.LLM_REASONING_OMIT=false --set env.LLM_TOP_P=0` and confirm the rendered Secret contains `LLM_REASONING_OMIT: "false"` and `LLM_TOP_P: "0"` (NOT omitted, NOT YAML booleans/numbers)
- [x] 6.10 Render `--set app.port=9000` and confirm `containerPort: 9000`, Service `targetPort: 9000`, both probe ports `9000`, AND the rendered Secret's `PORT` entry is `"9000"`
- [x] 6.11 Render `--set env.HTTP_ONLY=true` and confirm no `/certs` mount; render `--set env.HTTP_ONLY=TRUE` and confirm the `/certs` mount IS present (case-sensitive match to runtime contract)
- [x] 6.12 Render `--set serviceAccount.create=true` and confirm a ServiceAccount resource appears and the Deployment references it; render with defaults and confirm no ServiceAccount resource and no `serviceAccountName` field on the pod spec
- [x] 6.13 Render `--set prompts.enabled=true --set-file prompts.files.system\.md=./system.md` and confirm the ConfigMap mount lands at `/app/prompt-overrides` (NOT under `/app/playground`), AND the rendered Secret contains `PROMPT_FILE: "/app/prompt-overrides/system.md"`
- [x] 6.14 Render `--set persistence.storageClass=fast-ssd` and confirm `spec.storageClassName: fast-ssd` on the PVC; render without that flag and confirm the PVC has NO `storageClassName` field at all
- [x] 6.15 Build the container image locally with `podman build -t heart-reverie:helm-test .` and run a quick smoke test (`podman run --rm -p 18443:8443 -e LLM_API_KEY=test -e PASSPHRASE=test heart-reverie:helm-test`) to confirm the image still boots cleanly with the env vars the chart will inject — no chart-side regression on the runtime contract

## 7. Critique and Adjust

- [x] 7.1 Send the full chart (Chart.yaml, values.yaml, all templates, examples, README) to a rubber-duck critique and apply any findings that prevent a real install failure
- [x] 7.2 Re-run tasks 6.1–6.8 after applying critique fixes to confirm no regression
