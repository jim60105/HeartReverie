## 1. CSP img-src Update

- [x] 1.1 Add `blob:` to `img-src` directive in `reader-src/index.html` CSP meta tag (already done, commit 778a55b)
- [x] 1.2 Verify image thumbnails display without CSP errors in browser DevTools console (verified with browser testing)
