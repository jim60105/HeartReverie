# Delta Spec: state-security (renamed from state-patches-security)

## RENAMED Requirements

FROM: `openspec/specs/state-patches-security/`
TO: `openspec/specs/state-security/`

The spec directory is renamed from `state-patches-security` to `state-security` to reflect the `state-patches` plugin being renamed to `state`. No requirement content changes are needed — all references within requirements are to "the tool" (the `state-patches` binary), which is unchanged.
