## 1. Backend validation

- [x] 1.1 In `writer/routes/plugin-actions-validation.ts::validateModeCombo`, change the append-mode branch so that ONLY `appendTag === undefined` resolves to `{ ok: true, mode, appendTag: null }` (tagless append); a string matching `APPEND_TAG_RE` resolves to that string; and any other value — including an explicit `null`, a non-string value, or a string failing the regex (including `""`) — still returns `pluginActionProblems.invalidAppendTag()`. Keep the existing `replace` + non-`undefined` `appendTag` → `invalidReplaceCombo` rejection (so `replace` + `null` is also rejected). The append/replace handling of explicit `null` is thus symmetric (both reject it).
- [x] 1.2 Confirm `writer/routes/plugin-actions.ts` and `plugin-actions-preflight.ts` thread the now `string | null` validated `appendTag` through without re-narrowing it to a non-null string; update local types where the `!` non-null assertion is currently applied.

## 2. Backend write mode & finalisation

- [x] 2.1 In `writer/lib/chat-types.ts`, widen the `append-to-existing-chapter` `WriteMode` variant's `appendTag` field to `string | null` and update the accompanying JSDoc to describe the tagless append (no wrapper element).
- [x] 2.2 In `writer/lib/chat-chapter-finalize.ts::finalizeAppendToExisting`, branch on `appendTag === null`: when `null`, set `normalised = aiContent.trim()` (NO `normaliseAppendContent` call) and `wrapped = \`\n${normalised}\n\``; otherwise keep the existing `normaliseAppendContent` + `<{tag}>…</{tag}>` wrapping. Set the `post-response` `appendedTag` to the (possibly `null`) `appendTag`. Update the surrounding `FinalizeArgs`/local type (`appendTag: string` → `string | null`).
- [x] 2.3 **(D5 — hook payload type)** In `writer/types/hooks.ts`, widen `PostResponsePayload.appendedTag` from `readonly appendedTag?: string` to `readonly appendedTag?: string | null`; update its JSDoc to state it is `null` for a tagless plugin-action append and omitted otherwise. Compile-check that the other four `post-response` dispatch sites (`finalizeWriteNewChapter`, `finalizeReplace`, `finalizeContinueLastChapter`, discard) still satisfy the type (they omit the field).
- [x] 2.4 In `writer/routes/plugin-actions-execute.ts`, pass `appendTag: validatedAppendTag` (which may be `null`) into the `append-to-existing-chapter` write mode — remove the `!` non-null assertion.
- [x] 2.5 Verify `writer/types/plugin.ts` (`appendedTag: string | null`) and `writer/types/ws.ts` (`appendedTag: string | null`) need no change; keep `appendTag` optional on the request types in `plugin-actions-shared.ts`; confirm `writer/routes/ws-plugin-action.ts` continues to forward `msg.appendTag` (so an omitted tag arrives as `undefined`).

## 3. Frontend (confirm + regression-proof)

- [x] 3.1 Confirm `reader-src/src/types/index.ts` keeps `appendTag?: string` optional on the run-prompt options type and that the resolved result type already allows `appendedTag: string | null`.
- [x] 3.2 Confirm `reader-src/src/composables/useChatApi.ts::runPluginPrompt` only spreads/sets `appendTag` when `opts.appendTag !== undefined` on BOTH the WebSocket envelope and the HTTP body (no behavioural change expected — this task is verification plus the tests in §4).

## 4. Tests

- [x] 4.1 `tests/writer/lib/chat_chapter_finalize_test.ts` (or the closest existing finalize test): add a unit test for tagless append — given `appendTag: null` and `aiContent` containing two `<image>` blocks plus prose, assert the appended chapter delta is EXACTLY `\n{trimmed content}\n` (byte-for-byte: one leading newline, trimmed content verbatim, one trailing newline) with NO added wrapper and both `<image>` blocks intact, and that the dispatched `post-response` payload has `appendedTag: null`.
- [x] 4.2 Add a finalize test asserting tagless append does NOT strip a single leading/trailing `<image>` block (normalisation is skipped when `appendTag === null`).
- [x] 4.3 `tests/writer/routes/plugin_actions_test.ts`: add a route test that `append: true` with NO `appendTag` is accepted (resolves to tagless append) and returns `{ chapterUpdated: true, appendedTag: null }`; keep/extend the existing tests asserting `append: true` + malformed `appendTag` → 400 `plugin-action:invalid-append-tag`, `append: true` + explicit `appendTag: null` → 400 `plugin-action:invalid-append-tag`, `replace: true` + `appendTag: "X"` → 400 `plugin-action:invalid-replace-combo`, and `replace: true` + `appendTag: null` → 400 `plugin-action:invalid-replace-combo`.
- [x] 4.4 `tests/writer/routes/ws_coverage_test.ts` (or equivalent): add a WS-path test that a `plugin-action:run` with `append: true` and no `appendTag` performs a tagless append and emits `plugin-action:done` with `appendedTag: null`.
- [x] 4.5 `reader-src/src/composables/__tests__/useChatApi-runPluginPrompt.test.ts`: add tests asserting that `runPluginPrompt(file, { append: true })` (no `appendTag`) emits a WS envelope AND an HTTP body that each contain NO `appendTag` key (assert both paths separately, since they are constructed independently), while `{ append: true, appendTag: "X" }` still includes it on both.
- [x] 4.6 Add/extend a `post-response` consumer/type test (e.g. in `tests/writer/lib/`) asserting the widened `appendedTag?: string | null` carries `null` for a tagless append and is omitted for chat/replace/write-new, and grep in-repo `post-response` handlers (built-in plugins) to confirm none perform an unconditional non-null string op on `appendedTag`.

## 5. Docs & skills

- [x] 5.1 Update `docs/plugin-dev/action-buttons.md` to document `appendTag` as optional (opt-in via omission; explicit `null` is rejected) and describe the tagless append (verbatim append, no wrapper, `appendedTag: null`).
- [x] 5.2 Update the `runPluginPrompt` write-mode summary in `AGENTS.md` (action-button paragraph) to mention tagless append and that `post-response.appendedTag` is `null` for it.
- [x] 5.3 Update `.agents/skills/heartreverie-create-plugin/SKILL.md` and `references/hook-api.md` so the `appendTag` field is documented as optional, the `{ append: true }` (no tag) verbatim-append behaviour is described, and the `post-response` `appendedTag` is documented as `string | null` (`null` for tagless append).
- [x] 5.4 Reconcile the `plugin-hooks` capability spec prose with the widened type and the single agreed convention ("`appendedTag` is `null` only for a tagless plugin-action append; omitted for chat / write-new / continue / replace / discard"). The `plugin-hooks` delta (`specs/plugin-hooks/spec.md`) carries an `## ADDED` "Post-response appendedTag nullability" requirement AND a `## MODIFIED Requirements` section that re-states ONLY the "Post-response typed payload and usage field" requirement (the authoritative type declaration) with `appendedTag?: string | null` (was `appendedTag?: string`) plus one added tagless scenario — verified byte-faithful to the main spec otherwise. The large "Hook stages" requirement is deliberately NOT modified by the delta (a MODIFIED reproduction would risk dropping unrelated stage bullets on sync); its line-14 `post-response` prose is non-type-contradictory and is fully clarified by the ADDED requirement. The source-of-truth `writer/types/hooks.ts` JSDoc was also widened to `?: string | null`.

## 6. Verification gates

- [x] 6.1 Run `deno task fmt` and `deno task lint`; commit any formatting changes.
- [x] 6.2 Run `deno task test:backend` and `deno task test:frontend`; all green.
- [x] 6.3 Mandatory container verification: `scripts/podman-build-run.sh`, confirm clean startup (`podman logs heartreverie 2>&1 | grep -i "error\|warn"`), then exercise the new path with a `curl -H "X-Passphrase: …"` `POST /api/plugins/<plugin>/run-prompt` body `{ series, name, promptFile, append: true }` (no `appendTag`) against a test story and verify the chapter file gains the appended content with NO wrapper element and the response reports `appendedTag: null`.
