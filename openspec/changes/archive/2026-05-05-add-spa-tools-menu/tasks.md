## 1. Router & Layout Skeleton

- [x] 1.1 Add a `toolsChildren: RouteRecordRaw[]` exported array to `reader-src/src/router/index.ts` (parallel to `settingsChildren`) with two entries: `tools-new-series` (path `new-series`, lazy-loaded `QuickAddPage`, `meta.title = "快速新增"`) and `tools-import-character-card` (path `import-character-card`, lazy-loaded `ImportCharacterCardPage`, `meta.title = "ST 角色卡轉換工具"`).
- [x] 1.2 Add a `/tools` parent route to the router config: lazy-loaded `ToolsLayout`, `redirect: { name: "tools-new-series" }`, `children: toolsChildren`. Place it after the `/settings` route block and before the catch-all.
- [x] 1.3 Create `reader-src/src/components/ToolsLayout.vue` mirroring `SettingsLayout.vue`: ~200 px sidebar with `<router-link>` per `toolsChildren` entry (using `meta.title` as label and `active-class` for highlight), plus a `← 返回閱讀` back button reusing the existing last-reading-route helper, plus a `<router-view />` content area.
- [x] 1.4 Move the existing `router.afterEach` "last reading route" guard's predicate into a shared helper (e.g. `isReadingRoute(path)`) and extend the predicate to also exclude `path === "/tools"` and `path.startsWith("/tools/")`. Reuse the helper in both `SettingsLayout` and `ToolsLayout` back buttons.
- [x] 1.5 Add unit test `reader-src/src/router/__tests__/isReadingRoute.test.ts` covering: `/settings`, `/settings/foo`, `/tools`, `/tools/foo`, `/settings-archive/x`, `/tools-archive/x`, `/storyA/storyB`, `/`.

## 2. Header — Tools Menu Component

- [x] 2.1 Create `reader-src/src/composables/useTools.ts` exposing `tools` (computed from `toolsChildren`), `isOpen`, `open`, `close`, `toggle`. Implement document-level click-outside handling that registers only while `isOpen.value === true` and tears down on close + on `onScopeDispose`.
- [x] 2.2 Create `reader-src/src/components/ToolsMenu.vue` rendering the 🧰 button + dropdown panel as a header-descendant element (no `<Teleport>`), iterating `tools` for `<router-link>` entries, closing on click, on outside click, on Escape, and on route change. Apply `header-btn header-btn--icon` classes for visual parity with the settings button.
- [x] 2.3 Mount `<ToolsMenu />` in `AppHeader.vue` immediately adjacent to the existing settings (`⚙️`) button. Verify the header still meets the no-wrap rule at 360–767 px; constrain the dropdown panel max-width to fit at 360 px.
- [x] 2.4 Add `reader-src/src/components/__tests__/ToolsMenu.test.ts` covering: dropdown opens/closes, list reflects a stubbed `toolsChildren`, click navigates via `router.push({ name })`, outside click closes, Escape closes and refocuses the trigger, dropdown is rendered as a descendant of `<header>` (not `<body>`).

## 3. Character Card Parser Library

- [x] 3.1 Create `reader-src/src/types/character-card.ts` exporting `TavernCardV2`, `TavernCardV3` (V3 wraps V2 with `spec: "chara_card_v3"` plus `spec_version`), and the normalized `ParsedCharacterCard` shape (`name`, `description`, `personality`, `scenario`, `firstMes`, `mesExample`, `creatorNotes`, `systemPrompt`, `postHistoryInstructions`, `alternateGreetings: string[]`, `tags: string[]`, `creator`, `characterVersion`, `bookEntries: { name, keys, content }[]`).
- [x] 3.2 Create `reader-src/src/lib/character-card-parser.ts` with `parseCharacterCard(file: File): Promise<ParsedCharacterCard>`. Implement: 16 MiB pre-read size cap (throw `"檔案過大（>16 MiB）"`), `arrayBuffer()` read, PNG signature verification, chunk walk with `DataView` that detects truncated chunks (declared length extends past buffer end → throw `"PNG 區塊不完整"`), `tEXt` keyword/text split at first `0x00`, base64 decode + JSON parse with V3-wins-over-V2 precedence, `character_book.entries.length > 1000` safety cap (throw `"character_book.entries 超過 1000 筆，無法匯入"`), normalisation including `character_book.entries` → `bookEntries`. Throw documented `Error` messages for the no-data, non-PNG, and parse-failure cases.
- [x] 3.3 Add fixtures under `reader-src/src/lib/__fixtures__/`: a minimal `chara`-only PNG, a `ccv3`-only PNG, a both-present PNG, a non-PNG file, a PNG missing both keywords, a truncated PNG (header claims more bytes than remain), and a `character_book` over the safety cap. Generate them with a small helper (e.g. embed precomputed base64 PNG strings) so tests run without an image library.
- [x] 3.4 Add `reader-src/src/lib/__tests__/character-card-parser.test.ts` covering each fixture and the malformed base64 / malformed JSON / V3-bad-falls-back-to-V2 / oversized-file / truncated-chunk / over-cap-entries paths.

## 4. Quick-Add Tool

- [x] 4.1 Create `reader-src/src/components/QuickAddPage.vue` with the eight form controls in the exact order from the `quick-add-tool` spec, the `世界典籍檔案名稱` pre-fill of `world_info.md` (treated as a default placeholder, not as user input for activity detection), per-field labels, a single `建立` submit button, and a status region.
- [x] 4.2 Implement client-side validation: required series + story; per-group activity rule based on `(displayName && body)` only — filename ignored for activity test, auto-derived (or defaulted) when group is active and filename is blank; filename pattern `/^[^\\/:*?"<>|\u0000-\u001F]+\.md$/`; reject `..` substring and leading `.`/`_`; UTF-8 byte length ≤ 255. Auto-append `.md` if user-typed filename lacks the extension before validation.
- [x] 4.3 Implement filename derivation (`reader-src/src/lib/lore-filename.ts`, shared with the importer): NFC-normalise display name, replace `[\\/:*?"<>|\u0000-\u001F]` with `-`, collapse whitespace runs to `-`, trim leading/trailing `-`/`.`, append `.md`; preserve CJK Unified Ideographs / Hiragana / Katakana / Hangul verbatim. Fallback `character.md` / `world_info.md` when result is empty.
- [x] 4.4 Implement the submit orchestration: sequential `POST /api/stories/:series/:name/init`, optional character `PUT /api/lore/story/:series/:story/<filename>` (no `_lore/` segment in URL — the backend prepends it), optional world_info PUT, all using `useAuth().getAuthHeaders()`. Frontmatter body sent SHALL be exactly `{ enabled: true, priority: 0 }` (no `name`, no `tags`); display name lives in body H1 (`# <name>\n\n<body>`). Stop at first failure with the documented inline error messages. Treat 200 from init as success with non-blocking notice "已沿用現有故事資料夾". On full success `router.push({ name: "story", params: { series, story } })`.
- [x] 4.5 Implement collision preflight: before each active group's PUT, issue `GET /api/lore/story/:series/:story/<filename>`. On 200 surface "已存在同名典籍：<filename>" + an unchecked 覆寫現有典籍 checkbox beside the group; disable 建立 until every colliding group is acknowledged or the filename is changed (re-running preflight). On 404 proceed silently.
- [x] 4.6 Disable the submit button while requests are in flight; show "建立中…" plus current step in the status region.
- [x] 4.7 Add `reader-src/src/components/__tests__/QuickAddPage.test.ts` covering: required-field disable, partial-group block (display name without body, body without display name), both-empty submit issues only init *even with the world_info filename placeholder still showing*, character-only / world_info-only / both-active call sequences, frontmatter contains exactly `enabled` and `priority` (no `name`, no `tags`), URL never contains `/_lore/`, init-failure halts before any PUT, character-PUT-failure halts before world_info PUT, world_info-PUT-failure does not navigate, filename auto-append `.md`, path-traversal filename rejected, `_`-prefixed filename rejected, CJK display name yields a CJK filename, blank-name fallback to `character.md`, collision preflight 200 surfaces overwrite checkbox + blocks 建立, overwrite checkbox toggle re-enables 建立 and proceeds with PUT, init 200 surfaces non-blocking notice and proceeds.

## 5. Import-Character-Card Tool

- [x] 5.1 Create `reader-src/src/components/ImportCharacterCardPage.vue` with the three regions (file selector w/ drop-zone + `<input type="file" accept="image/png">`; editable form hidden until parsed; action region). Hydrate a deep-cloned form state from `parseCharacterCard()` output and stash a hydration snapshot for dirty-form detection.
- [x] 5.2 Render textareas for every scalar field, dynamic textareas for `alternateGreetings` (with add/remove), and a `<details>` collapsible block per `bookEntries` item with name/keys/body controls plus add/remove. Use `v-model` on every control bound to the form state (never to the parsed source).
- [x] 5.3 Render the series/story picker (series text input + story text input — same UX semantics as `StorySelector` for new-or-existing, but inline; do NOT auto-navigate on selection). Pre-fill the character lore filename input by calling the shared filename-derivation helper from task 4.3 against `form.name`; pre-fill the world_info filename input with `world_info.md`.
- [x] 5.4 Implement dirty-form guard on subsequent file pick: deep-compare current form state to the hydration snapshot; if equal parse silently, otherwise prompt "丟棄目前編輯並載入新檔案？" before parsing. Cancel preserves state and discards the file pick.
- [x] 5.5 Implement tag sanitisation (shared helper or inline) against the backend's `isValidTag` rule (non-empty, ≤ 100 chars, no `[`, `]`, `,`, `\n`, `\r`). Drop invalid tags from the PUT body, surface inline drop warnings, but keep `form.tags` untouched in the visible form state.
- [x] 5.6 Implement the 匯入 sequence: validate (including filename derivation/validation rules from task 4.2/4.3); collision preflight (same logic as Quick-Add task 4.5) for both character and world_info filenames; `POST /api/stories/:series/:story/init`; character lore PUT with body `{ frontmatter: { enabled: true, priority: 0, ...(sanitisedTags.length>0 && { tags: sanitisedTags }) }, content: "# <form.name>\n\n<assembled markdown>" }` (no `name` in frontmatter; URL has no `/_lore/` segment); if `bookEntries.length > 0` issue the world_info PUT with body content opening `# <form.worldInfoName>` and one `## <entry.name>` / `**Keys:**` / content section per entry. Skip empty scalar sections in the markdown assembly.
- [x] 5.7 Render parser errors inline above the file selector (covering oversized file, truncated chunk, over-cap entries, non-PNG, no-data). Render request errors inline in the action region. On full success `router.push({ name: "story", params: { series, story } })`.
- [x] 5.8 Add `reader-src/src/components/__tests__/ImportCharacterCardPage.test.ts` covering: V2-only PNG hydrates form; V3-only hydrates; V3+V2 prefers V3; missing-both displays error; non-PNG displays error; oversized-file displays error; truncated-PNG displays error; over-cap entries displays error; user-edited textarea content is what gets PUT (not the parsed values); CJK character name pre-fills CJK filename; blank name falls back to `character.md`; `bookEntries.length === 0` issues no world_info PUT; non-empty `bookEntries` produces the documented body; empty scalar fields are omitted from markdown headings; frontmatter never contains `name`; URL never contains `/_lore/`; tags absent from frontmatter when sanitised array is empty; tags with `,` / `[` / `]` / over-long are dropped with inline warnings; clean-form second pick replaces silently; dirty-form second pick prompts; cancel on dirty prompt preserves state; collision preflight 200 surfaces overwrite checkbox + blocks 匯入; acknowledged collision proceeds with PUT; no `multipart/form-data` request is ever made for the PNG bytes.

## 6. Wiring & Verification

- [x] 6.1 Run `deno task type-check` (or the equivalent `tsc --noEmit` task in `reader-src/`) and resolve any new type errors introduced by the new components, composable, parser, and types module.
- [x] 6.2 Run `deno task test` (and the frontend test runner) and confirm all new tests pass plus all pre-existing tests remain green; address any router/header tests that referenced "all top-level routes" if they need to learn about `/tools`.
- [x] 6.3 Run `deno task build:reader` to confirm the SPA bundles cleanly with the lazy-loaded `ToolsLayout`, `QuickAddPage`, and `ImportCharacterCardPage` chunks present.
- [x] 6.4 Manual smoke test: 🧰 dropdown opens/closes; navigate to `/tools/new-series`, create a series+story+character+world_info successfully; manually inspect that the resulting files in `playground/<series>/<story>/_lore/` (a) live at the expected scope-relative path with no double `_lore/`, and (b) carry only `tags`/`priority`/`enabled` in frontmatter; navigate to `/tools/import-character-card`, drop a sample SillyTavern card, edit a textarea, click 匯入, verify the *edited* text appears in the resulting lore markdown file and the display name is rendered as the body's first H1.
- [x] 6.5 Update `README.md` to mention the Tools menu and the two initial tools in the user-facing feature list. Update `AGENTS.md`'s component map to reference the new files.

## 7. Spec Sync

- [ ] 7.1 After merge / archive, sync the new specs (`tools-menu`, `quick-add-tool`, `import-character-card-tool`) into `openspec/specs/` and the modified deltas (`vue-router`, `page-layout`, `settings-page`) into their existing spec files using the openspec archive workflow.
- [ ] 7.2 Run `openspec validate --strict` on the resulting main specs to confirm everything still parses and there are no orphan requirements.
