## 1. Backend: `replace-last-chapter` write mode

- [ ] 1.1 Add `replace-last-chapter` variant to the `WriteMode` discriminated union in `writer/lib/chat-shared.ts` (`{ readonly kind: "replace-last-chapter"; readonly pluginName: string }`); update the JSDoc block above the union to describe the new mode (memory-buffered stream, no `pre-write`/`response-stream` hooks, atomic replace via `atomicWriteChapter`, abort/error rollback).
- [ ] 1.2 In `streamLlmAndPersist`'s pre-stream resolution block, add a `replace-last-chapter` branch that resolves `targetNum`/`chapterPath` the same way `append-to-existing-chapter` does (`listChapterFiles` → highest, throw `ChatError("no-chapter", …, 400)` on empty story).
- [ ] 1.3 In the mode-specific finalisation block, add a `replace-last-chapter` branch that calls `atomicWriteChapter(storyDir, "<padded>.md", aiContent.trimEnd() + "\n")`, re-reads the chapter file into `chapterContentAfter`, appends usage via `appendUsage`, logs `op: "replace"`, and dispatches `post-response` with `source: "plugin-action"`, `pluginName: writeMode.pluginName`, and the post-replace content.
- [ ] 1.4 Confirm the existing abort handling re-throws `ChatAbortError` BEFORE entering the finalisation switch so the `replace` branch is skipped on cancel; add an inline comment locking that ordering.
- [ ] 1.5 Add Deno unit tests in `writer/lib/chat-shared_test.ts` (or a new sibling) covering: successful replace overwrites the file atomically; abort mid-stream leaves the original content unchanged; LLM error mid-stream leaves the original content unchanged; empty story directory throws `ChatError("no-chapter")`; a usage record is appended whose persisted shape matches the existing `TokenUsageRecord` schema verbatim (`{ chapter, promptTokens, completionTokens, totalTokens, model, timestamp }` only — no new fields are introduced on the persisted record) and whose `chapter` field equals the polished chapter's number.

## 2. Backend: plugin-action route + WS handler

- [ ] 2.1 In `writer/routes/plugin-actions.ts`, add `replace?: boolean` to the body schema and to `PluginActionRequestArgs` (mirror the existing `appendTag` plumbing). Add `"draft"` to the `RESERVED_VARIABLE_NAMES` constant.
- [ ] 2.2 Add a new problem helper `pluginActionProblems.invalidReplaceCombo(detail?)` returning HTTP 400 with `type` slug `plugin-action:invalid-replace-combo`, and `pluginActionProblems.noChapter(detail?)` returning HTTP 400 with `type` slug `plugin-action:no-chapter` (or reuse the existing `no-chapter` ChatError mapping if a problem helper is unnecessary).
- [ ] 2.3 In `runPluginActionWithDeps`, after the existing `mode` validation, accept the new tri-state derived from `(append, replace)`: reject `(true, true)` and `(false, true) + appendTag` with `invalidReplaceCombo`; map `(false, true)` → `replace-last-chapter`, `(true, false)` → `append-to-existing-chapter`, `(false, false)` → `discard`.
- [ ] 2.4 When the resolved mode is `replace-last-chapter`, after acquiring the generation lock and BEFORE calling `buildPromptFromStory`, load the highest-numbered chapter file's content from `storyDir` (use `listChapterFiles`), pass it through `pluginManager.getStripTagPatterns()` (the combined `promptStripTags` regex; no-op when the regex is `null`) the same way `chat-shared.ts`/`story.ts::stripPromptTags` already does for chapter history, and merge `{ draft: <stripped content> }` into `extraResult.value`. If the directory has no chapter files, return `{ ok: false, … }` with the `plugin-action:no-chapter` problem and release the lock.
- [ ] 2.5 Build the `WriteMode` literal `{ kind: "replace-last-chapter", pluginName }` for the new branch and pass it into `streamLlmAndPersist`.
- [ ] 2.6 Extend `PluginRunPromptResponse` in `writer/types.ts` with `chapterReplaced: boolean`; populate it in the route's success branch (`writeMode.kind === "replace-last-chapter"` ⇒ `true`, else `false`).
- [ ] 2.7 Update the HTTP handler at the bottom of `writer/routes/plugin-actions.ts` to forward `body.replace` into `runPluginActionWithDeps`. Update the WebSocket dispatcher in `writer/routes/ws.ts` to forward the new field on the `plugin-action:run` envelope and to include `chapterReplaced` on the `plugin-action:done` envelope.
- [ ] 2.8 Add Deno unit tests covering the new validator branches: `(append=true, replace=true)` ⇒ `invalid-replace-combo`; `(replace=true, appendTag=…)` ⇒ `invalid-replace-combo`; `extraVariables.draft` ⇒ `extra-variables-collision` regardless of mode; `replace=true` against an empty story ⇒ `no-chapter`; `replace=true` happy path ⇒ `chapterReplaced: true` and the chapter file content equals the streamed content. Add a strip-tags test: with the `context-compaction` plugin (and any other plugin contributing `promptStripTags`) loaded, a chapter file whose content contains `<user_message>玩家輸入</user_message>` and `<chapter_summary>舊摘要</chapter_summary>` blocks results in a `draft` Vento variable that contains neither tag block (the regex from `pluginManager.getStripTagPatterns()` removed both wrappers) while preserving the surrounding prose.
- [ ] 2.9 Add an integration test driving the full HTTP path (or WS path) end-to-end against a fixture story with a single chapter, asserting atomic replacement and post-response hook invocation.

## 3. Bundled `polish` plugin

- [ ] 3.1 Create `plugins/polish/plugin.json` with `name: "polish"`, `type: "prompt-only"`, a single `actionButtons` entry `{ id: "polish", label: "✨ 潤飾", visibleWhen: "last-chapter-backend", priority: 200, tooltip: "以文學筆觸重寫當前章節" }`, and no `frontendModule` / `backendModule` / strip-tags arrays.
- [ ] 3.2 Author `plugins/polish/polish-instruction.md` as a Vento template containing exactly one `{{ message "system" }}` block (literary modern Chinese prose; dialogue-driven; show don't tell; smooth transitions; faithful character voices; full-width Chinese punctuation; plain prose — no bullets/headings/preamble) and exactly one `{{ message "user" }}` block that wraps `{{ draft }}` in `<draft>…</draft>` and instructs the model to return only the rewritten chapter. Keep the file SFW; do NOT include any 18+/jailbreak/age-related/no-content-restrictions/do-not-disclose-prompt language.
- [ ] 3.3 Create `plugins/polish/README.md` documenting the action button, the atomic-replace semantics, the cancel-rollback guarantee, and the recommendation to branch the story before running Polish if the user wants to keep the original draft.
- [ ] 3.4 Add a backend test (or extend the plugin-loader test) asserting that the `polish` plugin auto-discovers, exposes one `actionButtons` descriptor with the correct `id`/`visibleWhen`, and surfaces in `GET /api/plugins`.
- [ ] 3.5 Run a content-lint test (regex check) over `plugins/polish/polish-instruction.md` rejecting any of `18+`, `NSFW`, `RPJB`, `no content restrictions`, `jailbreak`, `bypass`, `DO NOT DISCLOSE`, or `age` (case-insensitive) to enforce the SFW guarantee.

## 4. Frontend: option plumbing

- [ ] 4.1 In `reader-src/src/types/index.ts` (or wherever `RunPluginPromptOptions` lives) add `replace?: boolean` and add `chapterReplaced: boolean` to `RunPluginPromptResult` / the `plugin-action:done` typed envelope.
- [ ] 4.2 In `reader-src/src/composables/useChatApi.ts`, forward `opts.replace` on both the WebSocket `plugin-action:run` envelope and the HTTP `POST` body, and surface `msg.chapterReplaced` on the resolved result. Make sure `replace` and `append` are forwarded as independent fields (no normalisation collapsing them).
- [ ] 4.3 Update existing `useChatApi-runPluginPrompt` tests to cover: `replace: true` is forwarded over WS; `replace: true` is forwarded over HTTP; `chapterReplaced` is surfaced from the `plugin-action:done` envelope.
- [ ] 4.4 Verify in `reader-src/src/composables/usePluginActions.ts` that the curried `runPluginPrompt` wrapper still passes through the `replace` option without filtering — extend a unit test to assert this for the polish button.
- [ ] 4.5 Lift the chapter-editor state (`isEditing` + `editBuffer` predicate "has unsaved buffer for chapter N") out of `reader-src/src/components/ChapterContent.vue` into a shared composable / module-level reactive store so `usePluginActions` (and any other plugin-action consumer) can read it without prop drilling. Keep the existing `editChapter` save flow intact.
- [ ] 4.6 In `usePluginActions` (or the action-bar layer), include the editor's `hasUnsavedBufferForLastChapter` predicate when computing the `disabled` state of any action button whose plugin descriptor would dispatch a `replace: true` round (for v1: the `polish` button). Click handlers SHALL no-op while disabled and SHALL surface a tooltip / toast such as "請先儲存或捨棄章節編輯內容後再潤飾".
- [ ] 4.7 In the `runPluginPrompt` resolution path, when the resolved result has `chapterReplaced === true` and the editor is open on the replaced chapter, force `isEditing = false`, clear `editBuffer = ""`, and trigger a chapter content reload via the existing chapter-fetch pathway BEFORE settling the helper's promise.
- [ ] 4.8 Add frontend tests covering: the polish button is disabled when `isEditing && editBuffer !== ""` for the last chapter and a click is a no-op; on a successful `replace: true` round the editor's `isEditing` flips to `false`, `editBuffer` is cleared, and the chapter is re-fetched; on a `replace: true` round that errors or aborts, the editor state is left untouched.

## 5. Documentation & verification

- [ ] 5.1 Update `AGENTS.md` (Project Structure section under `plugins/`) to mention `plugins/polish/` and its purpose.
- [ ] 5.2 Update any developer-facing plugin-action documentation (e.g., the "Plugin run-prompt backend route" notes) to mention the new `replace` flag, the `draft` reserved variable, and the atomic-replace guarantee.
- [ ] 5.3 Run `deno task check` (or the project's existing typecheck task) and `deno task test` (backend) to verify no regressions.
- [ ] 5.4 Run the frontend test task (e.g., `npm run test:unit` inside `reader-src/`) to verify no regressions.
- [ ] 5.5 Manual integration test: with a backend story that has at least one chapter, click the `潤飾` button, verify the chapter is replaced atomically, the original is preserved on Stop, and the usage panel shows new tokens recorded against the polished chapter (existing `TokenUsageRecord` schema, no new fields). Then open the chapter editor, type into the buffer without saving, and verify the polish button is now disabled with the expected tooltip; save or discard the buffer and verify the polish button re-enables. Finally, with the polish button enabled, dispatch a polish round with the editor closed; confirm the rendered chapter updates immediately on completion. Repeat with the editor opened mid-stream (WS path) and confirm the editor is force-closed and the chapter reloaded once `chapterReplaced: true` arrives.
- [ ] 5.6 Run `openspec validate polish-last-chapter --strict` (if available) and ensure the change validates cleanly before opening the PR.
