## MODIFIED Requirements

### Requirement: Template variables

The Vento-based system prompt template (`playground/_prompts/system.md`) defines the template variable contract and prompt structure.

(No functional changes to template variables or prompt structure — only the default file path reference changes from `playground/prompts/system.md` to `playground/_prompts/system.md`.)
